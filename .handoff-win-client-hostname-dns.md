# Handoff: Windows client DNS hostname fix

**For:** Windows Agent  
**From:** Linux Agent  
**Status:** Awaiting Windows Agent implementation — see PR description for starter diff.

This file is a handoff marker and should be **deleted** when the Windows Agent
completes the fix and opens its own PR.
